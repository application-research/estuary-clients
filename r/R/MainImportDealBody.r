# Estuary API
#
# This is the API for the Estuary application.
#
# OpenAPI spec version: 0.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' MainImportDealBody Class
#'
#' @field coluuid 
#' @field dealIDs 
#' @field dir 
#' @field name 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MainImportDealBody <- R6::R6Class(
  'MainImportDealBody',
  public = list(
    `coluuid` = NULL,
    `dealIDs` = NULL,
    `dir` = NULL,
    `name` = NULL,
    initialize = function(`coluuid`, `dealIDs`, `dir`, `name`){
      if (!missing(`coluuid`)) {
        stopifnot(is.character(`coluuid`), length(`coluuid`) == 1)
        self$`coluuid` <- `coluuid`
      }
      if (!missing(`dealIDs`)) {
        stopifnot(is.list(`dealIDs`), length(`dealIDs`) != 0)
        lapply(`dealIDs`, function(x) stopifnot(is.character(x)))
        self$`dealIDs` <- `dealIDs`
      }
      if (!missing(`dir`)) {
        stopifnot(is.character(`dir`), length(`dir`) == 1)
        self$`dir` <- `dir`
      }
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
    },
    toJSON = function() {
      MainImportDealBodyObject <- list()
      if (!is.null(self$`coluuid`)) {
        MainImportDealBodyObject[['coluuid']] <- self$`coluuid`
      }
      if (!is.null(self$`dealIDs`)) {
        MainImportDealBodyObject[['dealIDs']] <- self$`dealIDs`
      }
      if (!is.null(self$`dir`)) {
        MainImportDealBodyObject[['dir']] <- self$`dir`
      }
      if (!is.null(self$`name`)) {
        MainImportDealBodyObject[['name']] <- self$`name`
      }

      MainImportDealBodyObject
    },
    fromJSON = function(MainImportDealBodyJson) {
      MainImportDealBodyObject <- jsonlite::fromJSON(MainImportDealBodyJson)
      if (!is.null(MainImportDealBodyObject$`coluuid`)) {
        self$`coluuid` <- MainImportDealBodyObject$`coluuid`
      }
      if (!is.null(MainImportDealBodyObject$`dealIDs`)) {
        self$`dealIDs` <- MainImportDealBodyObject$`dealIDs`
      }
      if (!is.null(MainImportDealBodyObject$`dir`)) {
        self$`dir` <- MainImportDealBodyObject$`dir`
      }
      if (!is.null(MainImportDealBodyObject$`name`)) {
        self$`name` <- MainImportDealBodyObject$`name`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "coluuid": %s,
           "dealIDs": [%s],
           "dir": %s,
           "name": %s
        }',
        self$`coluuid`,
        lapply(self$`dealIDs`, function(x) paste(paste0('"', x, '"'), sep=",")),
        self$`dir`,
        self$`name`
      )
    },
    fromJSONString = function(MainImportDealBodyJson) {
      MainImportDealBodyObject <- jsonlite::fromJSON(MainImportDealBodyJson)
      self$`coluuid` <- MainImportDealBodyObject$`coluuid`
      self$`dealIDs` <- MainImportDealBodyObject$`dealIDs`
      self$`dir` <- MainImportDealBodyObject$`dir`
      self$`name` <- MainImportDealBodyObject$`name`
    }
  )
)
