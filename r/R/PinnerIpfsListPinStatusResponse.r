# Estuary API
#
# This is the API for the Estuary application.
#
# OpenAPI spec version: 0.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' PinnerIpfsListPinStatusResponse Class
#'
#' @field count 
#' @field results 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PinnerIpfsListPinStatusResponse <- R6::R6Class(
  'PinnerIpfsListPinStatusResponse',
  public = list(
    `count` = NULL,
    `results` = NULL,
    initialize = function(`count`, `results`){
      if (!missing(`count`)) {
        stopifnot(is.numeric(`count`), length(`count`) == 1)
        self$`count` <- `count`
      }
      if (!missing(`results`)) {
        stopifnot(is.list(`results`), length(`results`) != 0)
        lapply(`results`, function(x) stopifnot(R6::is.R6(x)))
        self$`results` <- `results`
      }
    },
    toJSON = function() {
      PinnerIpfsListPinStatusResponseObject <- list()
      if (!is.null(self$`count`)) {
        PinnerIpfsListPinStatusResponseObject[['count']] <- self$`count`
      }
      if (!is.null(self$`results`)) {
        PinnerIpfsListPinStatusResponseObject[['results']] <- lapply(self$`results`, function(x) x$toJSON())
      }

      PinnerIpfsListPinStatusResponseObject
    },
    fromJSON = function(PinnerIpfsListPinStatusResponseJson) {
      PinnerIpfsListPinStatusResponseObject <- jsonlite::fromJSON(PinnerIpfsListPinStatusResponseJson)
      if (!is.null(PinnerIpfsListPinStatusResponseObject$`count`)) {
        self$`count` <- PinnerIpfsListPinStatusResponseObject$`count`
      }
      if (!is.null(PinnerIpfsListPinStatusResponseObject$`results`)) {
        self$`results` <- lapply(PinnerIpfsListPinStatusResponseObject$`results`, function(x) {
          resultsObject <- PinnerIpfsPinStatusResponse$new()
          resultsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          resultsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "count": %d,
           "results": [%s]
        }',
        self$`count`,
        lapply(self$`results`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(PinnerIpfsListPinStatusResponseJson) {
      PinnerIpfsListPinStatusResponseObject <- jsonlite::fromJSON(PinnerIpfsListPinStatusResponseJson)
      self$`count` <- PinnerIpfsListPinStatusResponseObject$`count`
      self$`results` <- lapply(PinnerIpfsListPinStatusResponseObject$`results`, function(x) PinnerIpfsPinStatusResponse$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
