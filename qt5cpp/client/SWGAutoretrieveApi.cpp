/**
 * Estuary API
 * This is the API for the Estuary application.
 *
 * OpenAPI spec version: 0.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

#include "SWGAutoretrieveApi.h"
#include "SWGHelpers.h"
#include "SWGModelFactory.h"
#include "SWGQObjectWrapper.h"

#include <QJsonArray>
#include <QJsonDocument>

namespace Swagger {

SWGAutoretrieveApi::SWGAutoretrieveApi() {}

SWGAutoretrieveApi::~SWGAutoretrieveApi() {}

SWGAutoretrieveApi::SWGAutoretrieveApi(QString host, QString basePath) {
    this->host = host;
    this->basePath = basePath;
}

void
SWGAutoretrieveApi::adminAutoretrieveInitPost(QString* addresses, QString* pub_key) {
    QString fullPath;
    fullPath.append(this->host).append(this->basePath).append("/admin/autoretrieve/init");



    SWGHttpRequestWorker *worker = new SWGHttpRequestWorker();
    SWGHttpRequestInput input(fullPath, "POST");

    if (addresses != nullptr) {
        input.add_var("addresses", *addresses);
    }
    if (pub_key != nullptr) {
        input.add_var("pubKey", *pub_key);
    }




    foreach(QString key, this->defaultHeaders.keys()) {
        input.headers.insert(key, this->defaultHeaders.value(key));
    }

    connect(worker,
            &SWGHttpRequestWorker::on_execution_finished,
            this,
            &SWGAutoretrieveApi::adminAutoretrieveInitPostCallback);

    worker->execute(&input);
}

void
SWGAutoretrieveApi::adminAutoretrieveInitPostCallback(SWGHttpRequestWorker * worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    }
    else {
        msg = "Error: " + worker->error_str;
    }

    QString json(worker->response);
    QString* output = static_cast<QString*>(create(json, QString("QString")));
    auto wrapper = new SWGQObjectWrapper<QString*> (output);
    wrapper->deleteLater();
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit adminAutoretrieveInitPostSignal(output);
    } else {
        emit adminAutoretrieveInitPostSignalE(output, error_type, error_str);
        emit adminAutoretrieveInitPostSignalEFull(worker, error_type, error_str);
    }
}

void
SWGAutoretrieveApi::adminAutoretrieveListGet() {
    QString fullPath;
    fullPath.append(this->host).append(this->basePath).append("/admin/autoretrieve/list");



    SWGHttpRequestWorker *worker = new SWGHttpRequestWorker();
    SWGHttpRequestInput input(fullPath, "GET");





    foreach(QString key, this->defaultHeaders.keys()) {
        input.headers.insert(key, this->defaultHeaders.value(key));
    }

    connect(worker,
            &SWGHttpRequestWorker::on_execution_finished,
            this,
            &SWGAutoretrieveApi::adminAutoretrieveListGetCallback);

    worker->execute(&input);
}

void
SWGAutoretrieveApi::adminAutoretrieveListGetCallback(SWGHttpRequestWorker * worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    }
    else {
        msg = "Error: " + worker->error_str;
    }

    QString json(worker->response);
    QString* output = static_cast<QString*>(create(json, QString("QString")));
    auto wrapper = new SWGQObjectWrapper<QString*> (output);
    wrapper->deleteLater();
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit adminAutoretrieveListGetSignal(output);
    } else {
        emit adminAutoretrieveListGetSignalE(output, error_type, error_str);
        emit adminAutoretrieveListGetSignalEFull(worker, error_type, error_str);
    }
}

void
SWGAutoretrieveApi::autoretrieveHeartbeatPost(QString* token) {
    QString fullPath;
    fullPath.append(this->host).append(this->basePath).append("/autoretrieve/heartbeat");



    SWGHttpRequestWorker *worker = new SWGHttpRequestWorker();
    SWGHttpRequestInput input(fullPath, "POST");




    if (token != nullptr) {
        input.headers.insert("token", "token");
    }

    foreach(QString key, this->defaultHeaders.keys()) {
        input.headers.insert(key, this->defaultHeaders.value(key));
    }

    connect(worker,
            &SWGHttpRequestWorker::on_execution_finished,
            this,
            &SWGAutoretrieveApi::autoretrieveHeartbeatPostCallback);

    worker->execute(&input);
}

void
SWGAutoretrieveApi::autoretrieveHeartbeatPostCallback(SWGHttpRequestWorker * worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    }
    else {
        msg = "Error: " + worker->error_str;
    }

    QString json(worker->response);
    QString* output = static_cast<QString*>(create(json, QString("QString")));
    auto wrapper = new SWGQObjectWrapper<QString*> (output);
    wrapper->deleteLater();
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit autoretrieveHeartbeatPostSignal(output);
    } else {
        emit autoretrieveHeartbeatPostSignalE(output, error_type, error_str);
        emit autoretrieveHeartbeatPostSignalEFull(worker, error_type, error_str);
    }
}


}
